const list = document.querySelectorAll("#categories");
const listNum = list.length;

console.log(list);
console.log(listNum);

// count.map();
// `Number of categories: ${}`

// `Category: ${h2}` // firstElementChild
// `Elements: ${}`

// `Category: ${h2}`
// `Elements: ${}`

// `Category: ${h2}`
// `Elements: ${}`
